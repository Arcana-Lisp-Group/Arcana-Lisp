opam-version: "2.0"

build: [make "-j%{jobs}%"]

run-test: [make "-j%{jobs}%" "test"]

install: [make "install"]

depends: [
  "coq" { >= "8.14~" }
  "coq-parsec" { >= "0.1.1" }
  "menhir" { >= "20220210" }
  "coq-menhirlib" { >= "20220210" }
]

tags: [
]
